# Base de données MongoDB
DATABASE_URL="mongodb+srv://<user>:<password>@cluster.mongodb.net/planify?retryWrites=true&w=majority"

# NextAuth.js Configuration
NEXTAUTH_SECRET="votre-secret-generez-en-un-avec-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Clés VAPID pour les notifications push
# Générez-les avec: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY="your_vapid_public_key"
VAPID_PRIVATE_KEY="your_vapid_private_key"

# Email (optionnel, pour les notifications)
SENDGRID_FROM="no-reply@planify.app"

# Variables d'environnement optionnelles
NODE_ENV="development"
